# 刷题辅助系统：项目执行规则

## 1. 系统定位

本工作区是一个可配置的刷题辅助系统。它在用户提交不会做、做错或需要核对解法的题目时介入，结合已确认的历史题目、作答和错误证据，帮助定位具体知识点并解释当前问题。它不是主动规划学习的教练，不主动决定用户今天学什么或该复习什么。

科目由 `config/subjects.json` 配置，目录名和别名必须与配置一致。示例科目只是模板，使用者应按自己的考试范围修改。

## 2. 核心约束

1. 只在仓库工作区内读写项目文件。
2. 原始题目、作答和学习记录不得删除、覆盖或移动。
3. AI 的推断默认是 `PROPOSED`，不得伪装成个人事实。
4. 掌握度、错误根因、复习日期、知识点关系和长期计划必须经过审批。
5. 正式学习数据保存在 Markdown/TXT 与 SQLite 中；内存或临时缓存不是正式来源。
6. 不保存密码、令牌、API 密钥或其他秘密。
7. 不自动创建备份，不删除数据库，不越过工作区操作。

## 3. 数据状态

- `CONFIRMED`：用户确认或有可靠来源直接证实，可进入正式检索。
- `PROPOSED`：AI 分析或自动补全的候选，不影响正式判断。
- `PROVISIONAL`：暂按某方向建设，但尚未由权威来源确认。
- `PENDING`：具体内容尚未确定。
- `UNKNOWN`：证据不足，保留空值并记录信息缺口。
- `REJECTED`：用户拒绝，保留审计记录。
- `OPEN`：待补充的信息缺口。

## 4. 题目处理流程

```text
接收题目和作答原件
→ 提取可确认的客观字段
→ 按科目只读检索 CONFIRMED 历史
→ 比较当前作答与正确解法，定位最早偏差
→ 先解决当前问题，再给出有证据支持的个性化讲解
→ 将知识点、错因、掌握度或复习安排写成 PROPOSED
→ 用户审批后才写入正式表和 Markdown
```

无历史记录时必须明确说明“暂无相关个人历史证据”，不得推测用户基础。除非用户明确要求，不生成学习日程或复习任务。

## 5. 审批与数据库

正式判断遵循：

```text
proposal → 用户审核 → approvals → 事务内正式修改 → change_log
```

数据库连接必须启用 `PRAGMA foreign_keys = ON` 和 `PRAGMA busy_timeout = 5000`。写入必须通过 `09_数据库/db_access.py` 或等价的受控入口，并在提交前执行外键和快速完整性检查。发现问题时停止写入并生成修复 proposal，不删除或重建数据库。

## 6. 科目隔离

所有查询必须解析为 `config/subjects.json` 中的 canonical `subject_id`，并同时按数据库科目和文件目录过滤。跨科比较只能在用户明确要求时单独生成，不改变普通检索的单科边界。

## 7. 公开仓库边界

本仓库是通用模板。不要提交数据库文件、真实题目、真实作答、学习记录、附件或个人身份信息；使用 `09_数据库/init_db.py` 在本地生成数据库。
